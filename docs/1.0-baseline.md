# RiftX 1.0 M0 Baseline Record

> Recorded as part of M0 exit criteria. Do not treat unchecked items as green.

## Source baseline

| Item | Value |
| --- | --- |
| Git commit at M0 start | `3135a61a0f6d4d94e517916dd44d49597dd05ed0` |
| M0 commits | `d681527` (gitignore), `9cd74de` (baseline) |
| Branch | `codex/v0.7-local-runtime` |
| Product docs baseline | v0.8 |
| Target release | `1.0.0` (see `1.0 计划.md`) |
| Version file | `VERSION` → `0.8.0` (Desktop / Tauri / package must match) |

## Toolchain (macOS local, `conda run -n agent`)

| Tool | Version |
| --- | --- |
| conda env | `agent` |
| Python | 3.12.11 |
| rustc | 1.95.0 (59807616e 2026-04-14) |
| Node | v26.3.0 |
| pnpm | 10.33.0 |
| Tauri (Cargo dep) | 2.x (`apps/desktop/src-tauri`) |

## Config policy

- Repo default: `riftx.toml` (safe OpenAI Responses example; no personal endpoints).
- Local overrides: `riftx.local.toml` (gitignored); start from `riftx.local.toml.example`.
- Launch with `RIFTX_CONFIG="$PWD/riftx.local.toml"` when using local overrides.

## Test execution log

| Suite | Status | Notes |
| --- | --- | --- |
| `pnpm install --frozen-lockfile` | **passed** | 2026-07-27, conda env `agent`, pnpm 10.33.0 |
| Desktop frontend build | **passed** | `pnpm --filter @riftx/desktop build` |
| RiftX crate directed `just test -p …` | **passed** | 139 passed, 0 skipped (~8.7s test / ~3.8m wall with compile) |
| `just test-desktop-bridge` | **passed** | 22 passed, 0 skipped (after `prepare:sidecar`) |
| Workspace full `just test` | **not run** | Requires explicit user approval per plan / AGENTS.md |
| GitHub Actions three-platform contracts | pending | After M0 commits are pushed |

Directed crates: `codex-riftx-app-server-adapter`, `artifacts`, `cli`, `core`, `credentials`, `crypto`, `domain`, `gateway`, `ipc`, `report`, `skills`, `tools`.

CI job split (M0.4): `upstream-lock`, `rust-fmt-check`, `app-server-protocol`, `riftx-crate-tests`, `local-contracts` (macOS/Windows/Linux), `desktop-frontend`, `tauri-bridge` (uses `just test-desktop-bridge`, not raw `cargo test`).
