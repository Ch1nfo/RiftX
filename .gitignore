# Dependencies and build output
node_modules/
.next/
.next-stale-design-options/
out/
coverage/
*.tsbuildinfo

# Local runtime state (RiftX and Pi)
.riftx/
*.jsonl

# Environment and credentials
.env
.env.*
!.env.example
.npmrc
.yarnrc
.yarnrc.yml
auth.json
models.json
*credentials*
*secrets*
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx

# Bundled skill data is distributable wordlists, not secrets — keep it tracked
# (e.g. SecLists' Common-Credentials/ dir matches *credentials* case-insensitively)
!recommended-skills/**

# Local databases and caches
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.cache/
.tmp/
tmp/

# Logs and OS/editor metadata
*.log
logs/
.DS_Store
.AppleDouble
.LSOverride
.idea/
.vscode/
test-results/
