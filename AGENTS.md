# 项目约定

- Agent 相关任务使用 conda 的 `agent` 环境运行和测试。
- 任何开发计划文档不要上传 Git。
- 仅在用户明确要求时构建 Docker 镜像或导出镜像包。日常代码修改只执行必要测试，不自动构建、重建或重新打包镜像。
- 不主动读取 `recommended-skills/` 内的技能正文、说明或附属文件内容。除非用户明确要求读取内容，否则仅可检查目录、路径、文件大小等元数据，或执行用户授权的复制、打包操作。分析技能加载机制时，只检查目录外的加载代码，不运行会把技能内容输出到当前对话的检查程序。
