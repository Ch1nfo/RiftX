/** Must match `codex_riftx_domain::AUTO_MODE_CONFIRMATION`. */
export const AUTO_MODE_CONFIRMATION = "AUTO MODE - TEST ENVIRONMENT ONLY";

/** Soft banner after this much silence during an Auto turn. */
export const AUTO_STALE_ACTIVITY_MS = 5 * 60 * 1000;
